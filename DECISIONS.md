# Decisions

Shared, agent-authored log of cross-cutting decisions the fleet must honor. The conductor records plan/steer decisions here; workers record interface contracts + adjustments. Re-read before each significant step.

## Plan approved
- **What:** Approved a 5-task plan for: ok so I want to make a tool that very nicely and maybe in a visual way tells me where my mac battery is going. I feel like I'm using up a lot. I use a lot of terminal based things but also maybe it's my browser. the native battery tool doesn't tell me much. I also have a lot of background processes. so lets first come up with a plan on what should be done. Tasks: Foundation: SwiftPM scaffold + BatteryCore (models, SQLite store, categories); Sampler daemon: powermetrics + IOKit collector, LaunchDaemon install scripts; Analytics engine: attribution, rollups, timelines, insights (BatteryCore/Analytics); Menu bar app: SwiftUI MenuBarExtra UI with charts + app bundling; Integration pass: end-to-end verify, seeded demo check, docs.
- **Why:** user-approved plan; workers implement these nodes in isolated workspaces, honoring the dependency edges
- **By:** conductor · 1786743808733

## n0: Scaffolded the SwiftPM workspace and built BatteryCore. Package.swift declares
- **Did:** Scaffolded the SwiftPM workspace and built BatteryCore. Package.swift declares library target BatteryCore, executables batteryscoped and BatteryScopeApp (both depending on BatteryCore), and test targets BatteryCoreTests + BatteryscopedTests. BatteryCore has Models.swift (BatterySample, ProcessSample, ProcessCategory — Codable/Sendable/Hashable), Categorizer.swift (pure rule-table classifier covering all required categories, case-insensitive, path-hint fallback, daemon-shape heuristic for background), and Store.swift (SQLiteStore over raw SQLite3 C API, WAL mode, schema v1 with battery_samples + process_samples tables and indexes, insert/prune/fetch API, BatteryScopePaths.defaultDBPath). batteryscoped is a usage-printing stub main.swift; BatteryScopeApp is a minimal @main SwiftUI stub. swift build and swift test both pass (29/29 tests green: categorizer coverage, model Codable round-trips, store round-trip/prune/WAL-concurrent-read/schema tests).
- **Interfaces:** Package.swift targets: BatteryCore (library), batteryscoped (executable, depends BatteryCore), BatteryScopeApp (executable, depends BatteryCore), BatteryCoreTests, BatteryscopedTests. BatteryCore public API: struct BatterySample, struct ProcessSample, enum ProcessCategory (browser/terminal/devtools/media/communication/system/background/other), enum Categorizer { static func categorize(name:bundlePathHint:) -> ProcessCategory }, enum BatteryScopePaths { static let defaultDBPath }, final class SQLiteStore { enum Mode { readWrite, readOnly }; static let schemaVersion; init(path:mode:) throws; insert(batterySample:) throws; insert(processSamples:) throws; prune(olderThanDays:now:) throws; batterySamples(from:to:) throws -> [BatterySample]; processSamples(from:to:) throws -> [ProcessSample] }. No external SwiftPM dependencies; uses system SQLite3 via `import SQLite3`.
- **Follow-ups:**
  - Implement real powermetrics/IOKit sampling in batteryscoped [out of lane] — main.swift is currently a stub that only prints usage; a later node owns sampling logic per task instructions
  - Implement analytics queries and real SwiftUI UI in BatteryScopeApp [out of lane] — BatteryScopeApp is a minimal placeholder view; UI/analytics are explicitly out of scope for this foundation node
- **By:** n0 · 2026-08-14T22:28:04.062Z

## n0: Resolved the 2-sided .gitignore merge conflict by unioning both
- **Did:** Resolved the 2-sided .gitignore merge conflict by unioning both sides (build/db/DS_Store ignores from n0 + Rudder orchestration ignores from the base). Foundation node n0 is complete and verified: swift build succeeds and swift test passes 29 tests, 0 failures. Package.swift declares all five targets (BatteryCore library, batteryscoped and BatteryScopeApp executables, BatteryCoreTests and BatteryscopedTests), so downstream nodes never need to edit it.
- **Interfaces:** Package.swift (5 targets, no external deps, macOS 14, swift-tools 5.9). Sources/BatteryCore/Models.swift: BatterySample, ProcessSample, ProcessCategory (browser/terminal/devtools/media/communication/system/background/other) - all Codable+Sendable. Sources/BatteryCore/Categorizer.swift: pure rule-table classifier over process name/path. Sources/BatteryCore/Store.swift: SQLiteStore(path:mode:) with .readWrite/.readOnly, WAL on, schema v1 (battery_samples, process_samples + indexes), insert(batterySample:), insert(processSamples:) batched in one transaction, prune(olderThanDays:), batterySamples(from:to:), processSamples(from:to:); BatteryScopePaths.defaultDBPath. Stubs: Sources/batteryscoped/main.swift (usage+exit), Sources/BatteryScopeApp/BatteryScopeApp.swift (@main SwiftUI App).
- **Follow-ups:**
  - powermetrics/IOKit sampling in batteryscoped [out of lane] — main.swift is still a usage-printing stub; the daemon loop and real sampling belong to a later node
  - Analytics queries and real SwiftUI UI [out of lane] — Store exposes only basic fetch; aggregation by category over time and the visual UI are owned by downstream nodes
- **By:** n0 · 2026-08-14T22:30:20.889Z

